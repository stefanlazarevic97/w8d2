Array.prototype.bubbleSort = function() {
    array = this.slice(1);
    let sorted = false;

    while(!sorted) {
        sorted = true;

        array.forEach( (ele, i) => {
            if (ele < arr[i + 1]) {
                new_ele
            }
        }
    }
};